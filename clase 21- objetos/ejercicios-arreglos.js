//1-FILTRAR
// tomar el siguiente arreglo:
var mix = [1,2,"casa",20,false,"pajaro",3,"auto"]
//recorrer(iterar) ese arreglo con un for loop y meter en el siguiente arreglo(res) todos los numeros que esten dentro del arreglo llamado mix:
var res = []
// de modo que despues de la iteracion el arreglo res nos quede como [1,2,20,3]
// pueden utilizar la funcionalidad typeof (buscar por internet)




//2-CONTAR NUMEROS PARES
// iterar el siguiente arreglo con numeros y contar los numeros pares:
var numeros = [12,39,5,10,100,9,22,33]
// utiliza esta variable para contar los numeros pares:
var cantidadDePares = 0
// Despues de iterar esta variable deberia tener el valor de 4
// Una vez que tengas el ejercicio completo, prueba con el siguiente arreglo buscando contar los numeros pares
var numeros2 = [3,4,13,10,2,83]
// recuerda volver a iniciar el valor de la variable cantidadDePares en 0
// al terminar el valor de la variable  cantidadDePares deberia ser 3


// 3-SUMAR
// debes iterar el siguiente arreglo:
var sumar = [10,5,8,30,11,27]
// debes sumar todos los numeros del arreglo
// utiliza esta variable para la suma
var sumaTotal = 0
// al finalizar la iteracion el valor de la variable sumaTotal deberia ser 91


//4-CONTAR VOCALES
// esta vez no van a iterar un arreglo, si no un string(funciona casi igual a un arreglo)
var string = "arroz"
// van a contar la cantidad de vocales que tiene este arreglo
// deben utilizar esta variable
var vocales = 0
// al finalizar la iteracion el valor de la variable vocales debe ser 2

//5-CONTAR CARACTERES
// iterar el siguiente arreglo
var strings = ["perro","pajaros","guitarra","elefante"]
// contar cuantos caracteres tiene cada palabra y meter el resultado dentro del siguiente arreglo
var caracteres =[]
//al finalizar la iteracion el arreglo caracteres deberia verse asi [4,7,8,8]




